# Subscribed channels file for pull_lineup.py --subscribed-file
#
# One VMS/real channel number per line (the number as it actually is on
# Fios, before any --number-offset). '#' starts a comment, either on its
# own line or trailing a number.
#
# Copy this to subscribed_channels.txt and fill in the channels your
# package actually includes -- anything not listed here comes back from
# the VMS's ContentDirectory but gets marked enabled=false, so dms_proxy.py
# won't put it in the guide or let it be streamed.
#
# Example:
# 52    # TBS
# 611   # WeatherHD
# 618   # FoxNewsHD
